<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RetroWorld - Play Classic Games</title>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .games-container {
            padding: 0;
            position: relative;
            z-index: 1;
            width: 100%;
            max-width: 100vw;
            overflow: hidden;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .header-section {
            height: 60vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem;
            text-align: center;
        }

        .description {
            color: #fff;
            font-size: 1.2rem;
            max-width: 800px;
            margin: 0 auto;
            line-height: 1.6;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeIn 1s ease-out 2s forwards;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .games-section {
            flex-grow: 1;
            padding: 2rem;
            background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.7));
        }

        .games-catalog {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 1.5rem;
            padding: 2rem;
            max-width: 1800px;
            margin: 0 auto;
        }

        .game-card {
            background: rgba(0, 0, 0, 0.7);
            border-radius: 8px;
            overflow: hidden;
            transition: transform 0.3s ease;
            position: relative;
            cursor: pointer;
            height: 100%;
            display: flex;
            flex-direction: column;
            aspect-ratio: 2/3;
        }

        .game-card:hover {
            transform: scale(1.02);
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
        }

        .game-image {
            width: 100%;
            height: 0;
            padding-bottom: 75%;
            position: relative;
            background-color: #2c3e50;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
            font-size: 0.8rem;
        }

        .game-image img {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .game-info {
            padding: 1rem;
            color: #fff;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }

        .game-title {
            font-size: 1rem;
            margin-bottom: 0.5rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .game-description {
            font-size: 0.8rem;
            color: #ccc;
            margin-bottom: 1rem;
            flex-grow: 1;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .play-button {
            display: inline-block;
            padding: 0.6rem 1rem;
            background-color: #e74c3c;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            transition: background-color 0.3s;
            font-size: 0.8rem;
            text-align: center;
            margin-top: auto;
        }

        .play-button:hover {
            background-color: #c0392b;
        }

        @media (max-width: 1600px) {
            .games-catalog {
                grid-template-columns: repeat(4, 1fr);
                max-width: 1400px;
            }
        }

        @media (max-width: 1200px) {
            .games-catalog {
                grid-template-columns: repeat(3, 1fr);
                max-width: 1000px;
            }
        }

        @media (max-width: 900px) {
            .games-catalog {
                grid-template-columns: repeat(2, 1fr);
                max-width: 700px;
            }
        }

        @media (max-width: 600px) {
            .games-catalog {s
                grid-template-columns: 1fr;
                max-width: 400px;
                padding: 1rem;
                gap: 1rem;
            }
        }

        .audio-control {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: rgba(0, 0, 242, 0.7);
            padding: 0.5rem;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .audio-control:hover {
            background: rgba(255, 0, 0, 0.9);
        }

        .audio-control i {
            color: #fff;
            font-size: 1.2rem;
        }
    </style>
</head>
<body>
    <video class="video-background" autoplay muted loop>
        <source src="/media/background.mp4" type="video/mp4">
    </video>
    <div class="overlay"></div>

    <audio id="bgMusic" class="audio-player" loop>
        <source src="/media/background.mp3" type="audio/mpeg">
    </audio>

    <header>
        <nav>
            <a href="/" class="active">Home</a>
            <a href="/about">About Us</a>
        </nav>
    </header>

    <main class="games-container">
        <section class="header-section">
            <h1 class="site-title">RetroWorld</h1>
            <p class="description">
                Step into a world of nostalgia with our collection of classic games. 
                From arcade favorites to retro adventures, experience gaming history 
                in its purest form.
            </p>
        </section>

        <section class="games-section">
            <div class="games-catalog">
                <% games.forEach(game => { %>
                    <div class="game-card">
                        <div class="game-image">
                            <img src="<%= game.image %>" alt="<%= game.title %>">
                        </div>
                        <div class="game-info">
                            <h3 class="game-title"><%= game.title %></h3>
                            <p class="game-description"><%= game.description %></p>
                            <a href="/game/<%= game.id %>" class="play-button">Play Now</a>
                        </div>
                    </div>
                <% }); %>
            </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2024 RetroWorld. All rights reserved.</p>
    </footer>

    <div class="audio-control" onclick="toggleAudio()">
        <i class="fas fa-volume-up"></i>
    </div>

    <script src="https://kit.fontawesome.com/a076d05399.js"></script>
    <script>
        window.addEventListener('load', function() {
            const audio = document.getElementById('bgMusic');
            audio.volume = 0.5;
            
            // Play audio immediately
            audio.play().catch(() => {
                // If autoplay fails, try on first user interaction
                document.addEventListener('click', () => {
                    audio.play();
                }, { once: true });
            });
        });

        function toggleAudio() {
            const audio = document.getElementById('bgMusic');
            const icon = document.querySelector('.audio-control i');
            
            if (audio.paused) {
                audio.play();
                icon.classList.remove('fa-volume-mute');
                icon.classList.add('fa-volume-up');
            } else {
                audio.pause();
                icon.classList.remove('fa-volume-up');
                icon.classList.add('fa-volume-mute');
            }
        }
    </script>
</body>
</html> 